apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-cud-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend-cud-service
                port:
                  number: 8080
