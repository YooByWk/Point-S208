apiVersion: v1
kind: Service
metadata:
  name: backend-cud-service
spec:
  selector:
    app: backend-cud
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31932  # NodePort 지정
  type: NodePort
